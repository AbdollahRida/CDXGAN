# SeqGAN for CDX generation

## Requirements

Python 3
TensorFlow >= 1.0.0

## Quick run

`$ bash train_CDX.sh`

## Comments

Looks like it compiles and runs, but I don't know if what I'm generating is good :/
